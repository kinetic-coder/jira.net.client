namespace jira.net.client.library.JiraUtilities;

using RestSharp;
using jira.net.client.library.DataTransferObjects;

public static class Jira {
    public static JiraResponse Send() 
    {
        return new JiraResponse();
    }
}